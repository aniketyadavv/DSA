/*
    You have given an array contains both positive and non-positive numbers, elements are in the range from 
    -MAX to +MAX. our task is to search if number is present in array or not in 0(1) time.

    How to handle negative numbers?
    to handle negative number we should create a 2D array....of size Hasharray[MAX+1][2]
    
*/

#include<iostream>
#include<
#define MAX 1000
using namespace std;


int main(){

}